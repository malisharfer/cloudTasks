name: 🧹 Pre-commit

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: 📁 Checkout repository
        uses: actions/checkout@v4

      - uses: pre-commit/action@v3.0.0
